import { Position } from './position';

/**
 * currently interface is empty as bullet doesn't
 * have any more client-side relevant data, f.e
 * when it will be nesseccery to show the speed of the bullet,
 * or have some visual effect based on it's speed, that's when
 * more data will be added to this interface
 */
export interface Bullet extends Position {
}
